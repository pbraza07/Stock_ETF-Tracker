# MarketScope v5.9.4 Upgrade

Upload v5.9.4 over the existing repository. This is a UI/data-presentation upgrade and does not require deleting or resetting persistent snapshot data.

The new Table View uses the same current snapshot and filters as Card View. It does not reintroduce columns intentionally removed in earlier versions.
