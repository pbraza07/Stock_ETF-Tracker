# Data source notice

MarketScope v3 uses Yahoo Finance data through the open-source `yfinance` client and does not require a paid market-data API key.

The dashboard uses a daily precomputed snapshot for fast startup. The optional Live Refresh and on-demand chart features contact Yahoo when requested. Yahoo availability, delays, throttling and terms apply. Free public website data should not be treated as a guaranteed consolidated exchange feed for order execution.
