# MarketScope v5.9.14 — In-card Clickable Profit Tiles

- All 15 return periods are clickable inside the instrument card.
- The selected-period ending value, profit, and return are displayed inside the same card.
- Uses a card-local Streamlit fragment so clicking a timeframe updates only that card area for a smooth interaction.
- Open / News / Holdings / Compare stay inside the same card border.
- Removed the legacy empty fixed-height HTML shell that made return buttons appear outside the square.
