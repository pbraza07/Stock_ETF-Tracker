# IMPORTANT UPGRADE — MarketScope v5.9.36

Comparison Card View now uses the same fixed three-column return-period tile layout as Market Navigator Card View on mobile and desktop. The change is CSS-scoped to comparison card containers and does not alter return calculations, click behavior, card pagination, or simulator logic.
